{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "deno",
      "command": "cache",
      "args": [
        "--config",
        "${workspaceFolder}/functions/deno.json",
        "--lock",
        "${workspaceFolder}/functions/deno.lock",
        "--lock-write",
        "${workspaceFolder}/functions/auth/index.ts",
      ],
      "problemMatcher": [
        "$deno"
      ],
      "group": "build",
      "label": "deno: cache"
    }
  ]
}